http:
  middlewares:
    my-ldapAuth:
      plugin:
        ldapAuth:
          Enabled: true
          LogLevel: "DEBUG"
          Url: "ldap://ldap.forumsys.com"
          Port: 389
          BaseDN: "dc=example,dc=com"
          Attribute: "uid"
